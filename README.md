# Git-Html-Viewer
Lightweight standalone viewer for any HTML file stored in a GitHub repo. No server required — renders reports directly from raw GitHub content. Share reports via link, no need to download files.
